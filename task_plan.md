# Task Plan

- [x] Copy attached images to the `public/images/logo` directory.
- [x] Update browser favicon (Right image).
- [x] Update the left-top main logo in the Header component (Right image).
- [x] Update user profile avatar component to use Right image for regular users, and Left image for Admin users.
- [x] Implement Light/Dark mode toggle (using `next-themes` and semantic variables to replace hardcoded colors like `bg-[#0a0a0a]`).
- [x] Add seller registration banner to the main page encouraging users to become sellers (Image applied, SELLER role hidden, Auth toast added).
